PORT=3000
# Set a strong secret key for JWT signing
JWT_SECRET=greenmove_hackathon_super_secret

# The backend is currently using SQLite. If switching to actual Postgres via 'pg',
# specify your Neon connection string here.
DATABASE_URL=postgres://user:password@hostname/dbname
